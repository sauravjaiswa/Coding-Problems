//Juggling Letters

#include<bits/stdc++.h>
#define lli long long int
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    lli n,i,j,flag=0;

    cin>>n;
    lli a[n];
    for(i=0;i<n;i++){
        cin>>a[i];
    }

    for(i=1;i<=)

}
