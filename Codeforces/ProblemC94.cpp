//ProblemC

#include <bits/stdc++.h>
#define lli long long int
using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin>>t;

    while(t--){
    	lli n,x,i,j;
    	string s,ns="";
    	char ch;
    	cin>>s;
    	cin>>x;
    	n=s.length();

    	for(i=0;i<n;i++){
            ch=s[i];

            if(((i+1)+x)<=n && s[(i+1)+x]=='1')
                ns[]
    	}
    }
}
